#!/usr/bin/make -f
export DH_VIRTUALENV_INSTALL_ROOT=/usr/share/python

%:
	dh_install --sourcedir nylas/services/dashboard/build
	dh $@ --with python-virtualenv

# https://github.com/spotify/dh-virtualenv/issues/203
override_dh_strip:
	dh_strip  --exclude=cffi
